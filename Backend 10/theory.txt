Data Storage:
    aisa kuch jaha par hum saare data store kar sakte hain
    Database
Types of DBS: SQL | NoSQL
SQL - saare data ko ek roop k niche - Tables
NoSQL - mongoDB  (Datas are stored in the form of objects)
Terminologies - collections, documents, schemes, keys, models

Backend : Application Server       and      Database Server
        (accepts request,                   (provides data when application 
        sab kuch karna except data)          server request database server) 

Database -> Collections -> Document ->

CODE
---------------------------------------

CODE                        DATABASE
mongoose.connect     ->    database create
model create         ->    collections
CREATE code          ->    document